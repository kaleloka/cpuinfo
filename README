CPU Info Test Program:

Coder: Kevin Ching

Tested Platform:
	Ubuntu x64 14.04 LTS
	Qt 5.5
	Qt Quick 2.0

Usage:
	- run CpuInfo
	- goto File->Display CPU Info to display CPU data

Files:
	CpuInfo:
		- runable binary, developed and tested base on the above platform
  ccpuinfo.h, ccpuinfo.cpp:
		- CCpuInfo class that reads CPU info on allocation and store in two lists
		- this class also allow qml module to access stored info.
	main.qml:
		- contains a ListView that displays the CPU data
		- contains a File menu that initialize the display of CPU data
	other files:
		- files generated by Qt Creator





